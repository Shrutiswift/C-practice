// t = no of testcases
// X = unit of smoke emitted by a bus
// Y = unit of smoke emitted by a car
// N = No of passengers
#include<stdio.h>
int main() {
    int t;
    int N, X, Y, Z;
    int m,i, j;
    scanf("%d",&t);
    while(t--) {
        scanf("%d%d%d", &N, &X, &Y);
        int K = 100*j+4*i;
        for(int i = 0; i<= N; i++){
            for (int j = 0; j<= N; i++){
                
                (K >= N)? m = (m, i*Y+j*X):0;
                printf("%d", m);
            }
        }
    }
    return 0;
}