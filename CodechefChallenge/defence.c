#include<stdio.h>
int main() {
    int t;
    scanf("%d", &t);
    while(t--) {
        int N;
        int a[1000];
        scanf("%d", &N);
        for (int i = 0; i < N; i++) {
            scanf("%d", &a[i]);
        }
        if(a[i] == [10,60])

    }
    return 0;
}